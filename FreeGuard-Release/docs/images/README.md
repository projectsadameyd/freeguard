# Screenshots

Place free-Guard dashboard screenshots here (e.g. `dashboard-dark.png`) and
reference them from the main `README.md` for a polished look.

Example:

```markdown
![Dashboard](docs/images/dashboard-dark.png)
```